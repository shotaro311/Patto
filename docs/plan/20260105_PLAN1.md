# プランニングドキュメント：Patto! MVP（macOS / iOS）

## 共通ルール（必読）

- 共通ルールはリポジトリルートの `AGENTS.md` を参照: `../../AGENTS.md`

## 今回の追加ルール / 例外（差分だけを書く）

- MVP対象: macOS + iOS
- AI: MVPで必須（文章編集のみ）。音声入力はMVP後。
- AI呼び出し: ユーザーAPIキー登録（端末内に保存）
- 認証: ローカル利用はログイン不要。同期を有効化する場合のみログイン必須。
- 初期同期: 同期を有効化した時、既存ローカルメモをクラウドへ自動取り込み
- クイック起動: macOSは装飾キーのダブルタップで即入力。iOSも同等に即入力導線（例: ロック画面/ウィジェット）を提供。
- クイック起動の挙動: 「新規メモ / 前回メモ」を設定で選択できる
- 同期の最新判定: `server_updated_at` を基準（端末時計ズレ対策）
- 配布方針: iOS/macOSともApp Store公開を前提。テスト配布はTestFlightを利用。
- アプリ名: App Store表示名は「Patto!」
- 価格: 初回リリースは無料で進める
- 配信地域: 全世界
- 年齢制限: 4+（広告/UGC/無制限Webアクセスなし）
- カテゴリ: 仕事効率化（Productivity）
- プライバシーポリシー: GitHub Pages（Patto リポジトリ / gh-pagesブランチ）で公開
- ストア文言: 日本語/英語の2言語で準備する
- サポートページ: GitHub Pages（/support）で公開
- 例外: なし

## 実装モード（単独 / 並列）

- 実装モード: 並列（2ストリーム）
- 判定理由（要点だけ）:
  - ローカル機能/UI と 同期/認証 を分割できる
  - 共有境界（モデル/設定）は契約で固定してから進める

### 並列（2ストリーム）

- Stream A（ローカル）:
  - 雛形（Flutter）/ ローカルメモ（CRUD/検索/Markdown）/ 設定（AIキー、クイック起動の挙動）
  - 受け入れ条件: ログイン不要でメモ作成〜検索まで動く、即入力導線がある
- Stream B（同期）:
  - Supabase（任意ログイン）/ 同期（初期取り込み/競合）
  - 受け入れ条件: 同期ONでログイン→既存ローカルが自動アップロード→別端末で読める

**作成日**: 2026-01-05  
**バージョン**: 1.0  
**ステータス**: 承認済み（実装中）  
**承認**: 2026-01-05（ユーザー指示により実装開始）

---

## ⚡ クイックリファレンス

**現在地**: Phase 5 - 仕上げ（性能/配布準備）  
**次のアクション**: App Store Connectの必須項目を埋めて「提出」（iPad 13"スクショ/ビルド選択/価格/配信権利/暗号化コンプライアンス）  
**ブロッカー**: なし  
**並行開発ログ**: `docs/00_old/pdev_20260105-2200_20260105` にアーカイブ済み  
**重要な決定事項**: 「同期ON時のみログイン必須（初期は自動取り込み）」「AIは文章編集のみ（ユーザーAPIキー）」「クイック起動で即入力（新規/前回を選択可）」「配布はiOS/macOSともApp Store公開、テストはTestFlight」「App Store表示名はPatto!」「初回リリースは無料」「配信地域は全世界」「年齢制限は4+」「カテゴリは仕事効率化（Productivity）」「プライバシーポリシーはGitHub Pagesで公開」

### フェーズ進捗

- [x] Phase 0: 要件更新・MVP境界確定
- [x] Phase 1: アプリ雛形 + ローカルメモ（CRUD/検索/Markdown）
- [x] Phase 2: 同期（任意ログイン）+ 競合（UI/時計ズレ対応まで）
- [x] Phase 3: AI文章編集
- [x] Phase 4: クイック起動（macOSダブルタップ / iOS導線）
- [🔄] Phase 5: 仕上げ（性能/配布準備）

---

## 要件の未確定（最大3つ）

- TODO: なし

---

## 変更メモ（2026-01-05）

- クイック起動時のメモ欄フォーカスを安定化（遅延/未読込時の再フォーカス）
- クイック起動の監視を「非アクティブ時」でも拾えるよう補強（macOSイベント監視）
- 外部ディスプレイ/別スペースでも表示されるようウィンドウ挙動を調整（moveToActiveSpace/canJoinAllSpaces）
- クイック起動時に最前面表示を強制（activate/orderFrontRegardless/遅延再フォーカス）
- 変更ファイル: `lib/presentation/screens/note_editor_pane.dart`, `macos/Runner/MainFlutterWindow.swift`

## 変更メモ（2026-01-06）

- 同期の競合解決UI（ローカル/クラウド選択）を追加
- 同期の最新判定を `server_updated_at` 基準に更新（時計ズレ対策）
- 同期アルゴリズムを「競合検出→適用/アップロード」の順に整理
- 保存確定時の自動同期を追加（同期ON時）
- macOSのサンドボックスに外部通信許可を追加（Supabase接続のEPERM対策）
- 同期サービスがログイン状態に追随するよう修正（auth state で再評価）
- macOSのクイック起動がシングルタップで発火しないよう修正
- 削除済みメモを14日後にパージする方針と実装を追加
- クイック起動「新規」は未保存のクイックメモを開く方式に変更（空メモ量産防止）
- クイックメモ周りのビルドエラー（import不足）を修正
- クイックメモ表示を即時化（遷移アニメなし / 二重起動防止）
- クイックメモのopen状態管理をNavigatorObserverへ移動（Riverpodのbuild中更新エラー回避）
- クイックメモ遷移はスライド維持しつつホームの露出を抑制
- タイトル編集（重複チェック付き）を追加、プレビュー機能を削除
- iOS: ウィジェット/ロック画面からクイックメモを起動できるように追加
- 変更ファイル: `lib/app.dart`, `lib/services/sync_service.dart`, `lib/data/repositories/note_repository.dart`, `lib/data/repositories/quick_memo_repository.dart`, `lib/presentation/screens/quick_memo_screen.dart`, `lib/presentation/screens/settings_screen.dart`, `lib/presentation/screens/note_editor_pane.dart`, `lib/presentation/providers/notes_providers.dart`, `lib/presentation/providers/sync_providers.dart`, `lib/presentation/providers/auto_sync_provider.dart`, `lib/presentation/providers/quick_memo_provider.dart`, `docs/requirement/requirements.md`, `docs/sample/supabase_schema.sql`, `macos/Runner/DebugProfile.entitlements`, `macos/Runner/Release.entitlements`, `macos/Runner/MainFlutterWindow.swift`, `ios/Runner/AppDelegate.swift`, `ios/Runner/Info.plist`, `ios/QuickMemoWidget/QuickMemoWidget.swift`, `ios/QuickMemoWidget/Info.plist`, `ios/Runner.xcodeproj/project.pbxproj`

## 変更メモ（2026-01-07）

- iOSウィジェットの`Info.plist`に`CFBundleExecutable`を追加（実機インストールエラー対策）
- iOSウィジェットの`Info.plist`から`NSExtensionPrincipalClass`を削除（WidgetKit拡張のインストールエラー対策）
- iOSウィジェットの背景API対応（containerBackground）を追加（ウィジェット表示エラー対策）
- iOSロック画面ショートカット（コントロール）用のControlWidgetを追加
- iOSロック画面コントロールのAppIntent型不一致エラーを修正
- iOSロック画面コントロールのアクションをOpenURLIntent直指定に変更
- iOSロック画面コントロールの起動要求をAppGroup経由で受け取り、起動時にクイックメモを開くよう修正
- iOSロック画面起動時のみノッチ風モーフィング演出を追加
- iOSロック画面コントロールのIntentをAppIntent（openAppWhenRun）に変更
- iOSロック画面コントロールのIntentをOpenIntent（AppEnum）に戻してopenAppWhenRunの動作を優先
- iOSロック画面コントロールの定義をアプリ本体ターゲットにも含めるよう分離（起動不発対策）
- iOSロック画面ショートカット起動の導通を確認（App Groups/Target Membershipの見直し）

## 変更メモ（2026-01-09）

- App Store表示名をPatto!に統一（iOS/macOS/Flutter/ドキュメント）
- プライバシーポリシーのドラフトを作成
- Lint警告の解消（未使用import/不要なアンダースコア）
- 変更ファイル: `ios/Runner/Info.plist`, `macos/Runner/Configs/AppInfo.xcconfig`, `ios/QuickMemoWidget/QuickMemoControl.swift`, `lib/app.dart`, `lib/presentation/screens/notes_home_screen.dart`, `README.md`, `docs/requirement/requirements.md`, `docs/plan/20260105_PLAN1.md`, `docs/sample/supabase_schema.sql`, `docs/requirement/privacy_policy.md`, `lib/presentation/screens/quick_memo_screen.dart`, `lib/presentation/providers/quick_memo_provider.dart`, `lib/presentation/screens/settings_screen.dart`

## 変更メモ（2026-01-12）

- タイトル入力後に本文由来タイトルで上書きされる問題を修正（タイトルが空のときのみ自動更新）
- タイトルを入力中に自動保存（Enter不要）するよう修正
- iOSスクリーンショットを指定サイズで再撮影し、App Store Connectに追加
- App Storeメタデータ（プロモーション用テキスト/概要/キーワード）を入力して保存
- 変更ファイル: `lib/data/repositories/note_repository.dart`, `lib/presentation/screens/note_editor_pane.dart`

## 変更メモ（2026-01-14）

- iOSビルド（1.0.0 (1)）をApp Store Connectへアップロード
- 暗号化コンプライアンス回答を実施（標準的な暗号化）
- 全世界配信方針だが、当面はフランス配信を外して提出を優先（書類アップロード回避）
- 「iPad 13インチ」スクリーンショット必須のため追加対応が必要

## 変更メモ（2026-01-15）

- macOS配布（TestFlight）向けに `macos/Runner/Info.plist` へ `LSApplicationCategoryType` を追加（Validation failed対策）
- macOSビルドをApp Store Connectへアップロード（dSYMアップロード警告あり。配布自体は継続可能）
- dSYMアップロード警告（libisar.dylib）対策として、macOS RunnerのArchive時にdSYMを生成するRun Scriptを追加

## 変更メモ（2026-01-16）

- macOS TestFlightのビルド（1.0.0 (1)）が「却下済み」になり、インストール不可。却下理由の確認と新ビルドの再アップロードが必要
- macOSクイック起動（装飾キーのダブルタップ）が「隠す/閉じるはできるが、出てこない」ことがあるため、Input Monitoring（入力監視）権限の要求（CGRequestListenEventAccess）+ イベント監視の補強（local/global + event tap併用、重複イベント抑制、前面判定=active+not hidden）を実施。ユーザー環境で挙動確認が必要
- TextFieldの装飾（InputDecoration）を共通化し、重複を削減
- ホーム画面のメモ一覧で右クリックメニューからタイトル変更/削除を追加
- 変更ファイル: `lib/presentation/widgets/app_input_decoration.dart`, `lib/presentation/screens/auth_screen.dart`, `lib/presentation/screens/notes_home_screen.dart`, `lib/presentation/screens/quick_memo_screen.dart`, `lib/presentation/screens/settings_screen.dart`, `lib/presentation/screens/note_editor_pane.dart`
- Xcodeのビルド環境で`dart-define`を読み込めるよう、iOS/macOSのxcconfigに`Secrets.xcconfig`の任意includeを追加
- ローカルの`Secrets.xcconfig`を作成して`DART_DEFINES`を設定（値はgit管理外）
- 変更ファイル: `ios/Flutter/Debug.xcconfig`, `ios/Flutter/Release.xcconfig`, `macos/Flutter/Flutter-Debug.xcconfig`, `macos/Flutter/Flutter-Release.xcconfig`, `.gitignore`, `ios/Flutter/Secrets.xcconfig`, `macos/Flutter/Secrets.xcconfig`
