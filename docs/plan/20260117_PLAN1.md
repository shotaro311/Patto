# プランニングドキュメント：Patto! 次期機能（Obsidian/Apple Watch除外） 要件固め

## 共通ルール（必読）

- 共通ルールはリポジトリルートの `AGENTS.md` を参照: `../../AGENTS.md`

## 今回の追加ルール / 例外（差分だけを書く）

- 対象プラットフォーム: macOS / iOS
- 今回の対象（やりたい）:
  - UIの色分け（カテゴリごとにアクセントカラー指定）
  - 文字数表示
  - ボイス入力（高精度）
  - フォルダ機能（メモ分類）
  - ホットキー追加オプション（クリップボード/選択テキスト反映など）
  - AI編集機能の拡張（右クリック/ショートカット、カスタムプロンプト最大3）
  - 全体的なUI/UX調整
- 今回の非対象（やらない）:
  - Obsidian連携（送信）
  - Apple Watch対応
- このPLANは「要件確定（+最小の設計）」までをスコープとする（実装は承認後）
- 例外: なし

## 実装モード（単独 / 並列）

- 実装モード: 単独
- 判定理由（要点だけ）:
  - まずは要件固めが中心で、分割しても合流メリットが小さいため

**作成日**: 2026-01-17  
**バージョン**: 1.0  
**ステータス**: ドラフト  
**承認**: 未（要レビュー）  

---

## ⚡ クイックリファレンス

**現在地**: Phase 0 - 要件確定（0%）  
**次のアクション**: 「確認事項（最大3点）」への回答 → PLAN更新 → 承認  
**ブロッカー**: なし  
**重要な決定事項**: 未（確定待ち）

### フェーズ進捗

- [🔄] Phase 0: 要件確定（確認事項の回答・受け入れ条件の確定）
- [ ] Phase 1: 最小設計（データ項目/画面/設定/ショートカットの仕様）
- [ ] Phase 2: 実装タスク分解（PR単位の切り方・順序）

---

## 要件概要（ドラフト）

### 実装する機能（対象）

1) UIの色分け（カテゴリごと）
2) 文字数表示
3) ボイス入力
4) フォルダ機能
5) ホットキー追加オプション
6) AI編集機能の拡張（カスタムプロンプト最大3）
7) UI/UX調整

### スコープ外（今回やらない）

- Obsidian送信
- Apple Watch

---

## 個別要件（決めることリスト）

> ここは「結論（決定事項）→受け入れ条件→メモ」の順で埋める

### 1) カテゴリ別アクセントカラー

- 決定事項: TODO
- 受け入れ条件（案）:
  - カテゴリを選ぶと、メモ（一覧/エディタ）の見た目にアクセントが反映される
  - 設定画面でカテゴリごとに色を変更できる
- メモ:
  - 「カテゴリ」と「フォルダ」を別概念にするか（同一視するか）を先に決める

### 2) 文字数表示

- 決定事項: TODO（対象=本文のみ/タイトル含む、表示位置、単位）
- 受け入れ条件（案）:
  - 入力に合わせて文字数が即時に更新される

### 3) ボイス入力（高精度）

- 決定事項: TODO（Apple純正のみ/AI系API併用、対応言語、失敗時UI）
- 受け入れ条件（案）:
  - マイク入力から本文へテキスト化できる
  - 権限が無い/失敗した場合に理由が分かる表示が出る

### 4) フォルダ機能

- 決定事項: TODO（1メモ=1フォルダ、階層の有無、未分類の扱い）
- 受け入れ条件（案）:
  - フォルダでメモを分類し、一覧で絞り込みできる

### 5) ホットキー追加オプション

- 決定事項: TODO（トリガー条件、対象OS、設定項目）
- 受け入れ条件（案）:
  - 非表示時に本文をクリップボードへコピー（ON/OFF可能）
  - テキスト選択中に起動すると、その内容を本文に反映
  - ホーム/設定で起動した場合の貼り付け挙動が仕様通り
- メモ:
  - 「他アプリで選択中のテキスト」の取得方法はOS制約が大きいので要調査

### 6) AI編集機能の拡張

- 決定事項: TODO（右クリック/ショートカット仕様、カスタムプロンプト3つの管理UI、Apple Intelligenceの扱い）
- 受け入れ条件（案）:
  - 右クリック/ショートカットでAI編集を呼び出せる
  - カスタムプロンプト（最大3）が実行できる（右クリック・ショートカット対応）

### 7) UI/UX調整

- 決定事項: TODO（改善対象の優先順位）
- 受け入れ条件（案）:
  - 主要導線（メモ作成/編集/検索/クイック起動）が迷わず使える

---

## 確認事項（最大3点）

1) 「カテゴリ」と「フォルダ」は別物ですか？（A: 別物 / B: 同じ概念として統一）
2) フォルダは階層が必要ですか？（A: 階層なし / B: 階層あり）
3) ボイス入力はまず「Apple純正」で開始してOKですか？（A: Apple純正のみ / B: AI系APIも最初から）

---

## リスクと対応策（ドラフト）

- テキスト選択の取得: OS制約が強い → まずは「クリップボード経由」を第一候補として要調査
- ボイス入力の品質/対応言語: 実機差が出る → 受け入れ条件に「最低限」を明記する
- Apple Intelligence（オフライン）: 端末/OS要件がある → 対応範囲を明記し、非対応時のフォールバックを用意する

---

## 完了条件（このPLANの完了）

- 上記「確認事項」が全て回答済み
- 各機能の「決定事項」と「受け入れ条件」が埋まっている
- スコープ外（Obsidian/Apple Watch）が明記され、関係者が合意している
