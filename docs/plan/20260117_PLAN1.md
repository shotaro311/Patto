# プランニングドキュメント：Patto! 次期機能（Obsidian/Apple Watch除外） 要件固め

## 共通ルール（必読）

- 共通ルールはリポジトリルートの `AGENTS.md` を参照: `../../AGENTS.md`

## 今回の追加ルール / 例外（差分だけを書く）

- 対象プラットフォーム: macOS（先行）/ iOS（詳細は後続）
- 今回の対象（やりたい）:
  - 文字数表示
  - クイックメモ（下書きInbox）改善（自動保存・下書き1件）
  - タグ/リンクによる自動整理（AIで補助）
  - ホットキー追加オプション（表示/非表示の通常キーバインド追加）
  - AI編集機能の拡張（右クリック/ショートカット、カスタムプロンプト最大6）
  - 全体的なUI/UX調整
- 今回の非対象（やらない）:
  - Obsidian連携（送信）
  - Apple Watch対応
  - フォルダ機能（メモ分類）※一旦保留（タグ/リンク自動整理を優先）
  - 色ラベル（カテゴリ別アクセントカラー）※要件未確定のため保留
  - 音声入力 ※不要になったため保留
- このPLANは「要件確定（+最小の設計）」までをスコープとする（実装は承認後）
- ただし、2026-01-19〜2026-01-20 に macOS向けで一部（文字数表示 / ホットキー追加オプション / AI編集拡張）を先行実装し、以降は「仕様 ↔ 実装」を同期しながら進める
- 例外:
  - iOSの詳細仕様（D&D、タブ上限、UI差分など）はmacOS実装完了後に確定する
  - Mac App Store前提のため、他アプリの選択テキスト反映/非表示時コピーは見送り

## 実装モード（単独 / 並列）

- 実装モード: 並列（2ストリーム想定）
- 判定理由（要点だけ）:
  - 下書きInbox（UI/フロー）と、自動整理（タグ/リンク/AI）の責務が分離できるため
  - ただし共有境界（データモデル/永続化/履歴・Undo）は先に単独で固める（基盤タスク）

**作成日**: 2026-01-17  
**バージョン**: 1.0  
**ステータス**: 進行中（要件確定を継続 / 一部実装済み）  
**承認**: 未（要レビュー）  

---

## ⚡ クイックリファレンス

**現在地**: Phase 0 - 要件確定（ホットキー関連は確定・反映済み）  
**次のアクション**: macOSで動作確認 → iOS詳細（後続）  
**ブロッカー**: なし  
**重要な決定事項**: まずはmacOS先行で確定する / クイックメモは「下書きInbox」として扱い、入力は自動保存（下書きは常に1件のみ） / 「保存（整理/確定）」で通常メモとして扱い、クイックメモ入力欄はまっさらに戻る / タイトルは基本自動生成（手動編集は可能、重複はエラーで保存不可） / タグ/リンクで整理し、AIは自動整理の補助として使う（手動タグと自動タグは分離管理） / 自動整理の既定は「提案（確認して適用）」モード（確定） / AIはローカル（Apple Intelligence）優先、外部APIは任意でON / 既存ホットキー挙動は変更しない / 表示・非表示ホットキーはダブルタップ/通常キーバインドで設定可能（グローバル、デフォルトはShiftダブルタップ、通常キーバインドのデフォルト割当なし） / Mac App Store前提のため他アプリ選択テキスト反映・非表示時コピーは見送り / iOS詳細はmacOS実装後に確定
**実装メモ**: 2026-01-19 macOS向け「文字数表示 / ホットキー追加オプション / AI編集拡張」をコード反映
**実装メモ**: 2026-01-20 Mac App Store前提のため、他アプリ選択テキスト反映/非表示時コピーを削除（機能見送り）
**実装メモ**: 2026-01-22 クイックメモを「下書きInbox（Isar）」に移行（下書きは常に1件、保存で通常メモ化・入力欄リセット）
**実装メモ**: 2026-01-22 タグ/リンク整理（提案モード）を追加（manual/auto分離、linksOut抽出、自動タグは提案→適用、AIタグ提案は任意）
**実装メモ**: 2026-01-22 ホームの「＋」はクイックメモを開く（下書きがある場合は保存/編集/破棄の選択を提示、ホームに「下書き」導線を表示）
**実装メモ**: 2026-01-23 カスタムプロンプトのインラインローディング/対象範囲表示が更新されない問題を修正（`lib/presentation/widgets/ai_prompt_presets_hover_menu.dart`）
**実装メモ**: 2026-01-23 OverlayEntryの再描画がbuild中に走る問題を回避（post-frameで再描画）
**実装メモ**: 2026-01-23 HoverメニューのOverlayをホストから直接同期（scope/runningIndex/presets）し、押下時に即ローディング表示
**実装メモ**: 2026-01-23 ホバー選択後にメニューを閉じ、プレイリストアイコン上でローディング表示（スコープはDivider上部）
**実装メモ**: 2026-01-23 スコープトグルの表示はホバー内では行わない方針に変更（UI撤去）

### フェーズ進捗

- [🔄] Phase 0: 要件確定（確認事項の回答・受け入れ条件の確定）
- [ ] Phase 1: 最小設計（データ項目/画面/設定/ショートカットの仕様）
- [ ] Phase 2: 実装タスク分解（PR単位の切り方・順序）

---

## 要件概要（ドラフト）

### 実装する機能（対象）

1) クイックメモ（下書きInbox）改善
2) タグ/リンクによる自動整理（AIで補助）
3) 文字数表示
4) ホットキー追加オプション
5) AI編集機能の拡張（カスタムプロンプト最大6）
6) UI/UX調整

### スコープ外（今回やらない）

- Obsidian送信
- Apple Watch
- フォルダ機能（メモ分類）※一旦保留（タグ/リンク自動整理を優先）
- 色ラベル（カテゴリ別アクセントカラー / 保留）
- 音声入力（保留）

---

## 個別要件（決めることリスト）

> ここは「結論（決定事項）→受け入れ条件→メモ」の順で埋める

### 0) 画面構成（ナビゲーション）

- 決定事項: 起動直後は常にクイックメモ画面（確定） / 検索・メモ一覧は「整理画面」として分離（確定）
- 受け入れ条件（案）:
  - アプリ起動時、最初にクイックメモが表示される
  - 整理画面（検索/メモ一覧/タグ整理）へ遷移できる

### 1) フォルダ機能

- ステータス: 保留（今回スコープ外）
- 決定事項: 階層なし（確定） / 1メモ=1フォルダ（確定） / 「未分類」固定フォルダ（確定） / フォルダの作成・変更・削除（確定） / フォルダ削除は警告の上で配下メモも削除（確定） / メモのフォルダ移動（確定）
- 受け入れ条件（案）:
  - フォルダでメモを分類し、一覧で絞り込みできる
  - メモを別フォルダへ移動できる
  - フォルダ削除時は警告を出し、同意した場合のみフォルダと配下メモを削除する
- メモ:
  - 「未分類」フォルダは削除不可/名称変更不可の想定（必要なら再確認）
  - フォルダ削除の警告では「削除されるメモ件数」を表示する
  - 方針変更: フォルダ整理は一旦保留し、タグ/リンクの自動整理を優先する（フォルダ仕様は後で再検討）

### 1.2) クイックメモ（下書きInbox）

- 決定事項: 入力は自動保存（確定） / 未整理の入力は「下書き」として扱う（確定） / 下書きは常に1件のみ（確定） / ホットキー起動時はクイックメモ（下書き）を開く（確定） / 「保存（整理/確定）」で通常メモとして扱い、クイックメモ入力欄はまっさらに戻る（確定） / ホームの「＋」はクイックメモを開く（確定） / 下書きがある状態で「＋」を押した場合は「保存/編集/破棄」を選べる（確定） / 下書きがある場合、ホームに「下書き」導線を表示する（確定）
- 受け入れ条件（案）:
  - 入力中にアプリを閉じても、下書きが復元される
  - 新しいアイデアを書き始めても、既存の下書きが上書きされない
  - 「保存（整理/確定）」後は、次の入力がすぐ始められる（入力欄が空になる）
- メモ:
  - 対象: まずはmacOS（確定）。iOSはmacOSで固めた後に確定する（TODO）
  - 下書き保持数（上限）: 1（確定）

### 1.3) タグ/リンクによる自動整理（AIで補助）

- 決定事項: メモごとにメタデータ（JSON）を保持（確定） / `manualTags` と `autoTags` を分離して管理（確定） / `[[リンク]]` 等のリンク記法で関連付けできる（確定） / 自動整理（タグ候補・要約・関連候補）は AI で補助（確定） / AIはローカル（Apple Intelligence）優先、外部APIは任意でON（確定）
- 受け入れ条件（案）:
  - ユーザーが何も操作しなくても、下書き/メモに自動でタグが付与される（自動タグ）
  - 手動タグ（ユーザーが付けたタグ）は、自動整理で勝手に消えない
  - 自動整理の結果は「提案」として扱われ、確認してから適用できる（既定）
- メモ:
  - 関連付け（近いメモ）の方式（ルールのみ/埋め込み併用など）は後で決める（TODO）

### 1.4) サイドバー（フォルダ+メモ一覧）

- ステータス: 保留（今回スコープ外）
- 決定事項:
  - メモ編集中画面の左上にサイドバー表示/非表示アイコン（確定）
  - サイドバーにはフォルダ一覧が縦に並ぶ（確定）
  - 各フォルダの右側にプルダウン（展開）アイコン（確定）
  - 展開するとフォルダ内のメモ一覧が下にスライドして表示（確定）
  - フォルダを右クリックで「名称変更/削除」（確定）
  - 展開したメモ一覧は「メモタイトルがずらっと並ぶ」表示（確定）
  - タイトルが長い場合はフルタイトルを確認できる（確定）
- 受け入れ条件（案）:
  - サイドバーの表示/非表示を切り替えできる
  - フォルダを展開すると、メモ一覧がアニメーション付きで表示される
  - フォルダ右クリックで名称変更/削除ができる（削除は警告あり）
  - メモ一覧でタイトルが長い場合でも、フルタイトルを確認できる
- メモ:
  - macOS: タイトルを0.5秒ホバーでフルタイトルをうっすら表示（ツールチップ等）
  - iOS: 詳細はmacOS実装後に決定（TODO）

### 2) 文字数表示

- 決定事項: 本文のみを対象（確定） / 表示は右下に小さく薄い文字（確定） / 設定で表示のON/OFFが可能（確定） / 単位は文字数のみ（確定） / 句読点・記号を除外してカウントするオプションあり（確定） / オプションON時は「（記号含まず）」を併記（確定）
- 受け入れ条件（案）:
  - 本文の入力に合わせて文字数が即時に更新される
  - 設定で文字数表示のON/OFFを切り替えられる
  - 句読点・記号を除外してカウントできる
  - オプションON時は「（記号含まず）」が併記される

### 5) ホットキー追加オプション

- 決定事項: 現状のホットキー（表示/非表示の挙動）は変更しない（確定） / 表示・非表示ホットキーはダブルタップと通常キーバインドの両方で設定可能（グローバル）（確定） / 表示・非表示ホットキーのデフォルトはShiftダブルタップ、通常キーバインドのデフォルト割り当ては無し（確定） / Mac App Store前提のため、他アプリ選択テキスト反映と非表示時コピーは見送り（確定）
- 受け入れ条件（案）:
  - 表示/非表示ホットキーは、ダブルタップまたは通常キーバインドでグローバルに設定できる
- メモ:
  - Mac App Storeのサンドボックス制約により、他アプリ選択テキスト反映は不可
  - 実装は「表示/非表示」のみ（他アプリの選択テキスト取得や、非表示時のコピーは実装しない）
  - 権限まわり（macOS）: 入力監視/アクセシビリティの許可対象は、実行している `Patto!.app`（XcodeのProductsなど）になる点に注意

### 6) AI編集機能の拡張

- 決定事項: Apple Intelligence（ローカル）を優先 / 非対応・品質不足時に外部APIへ差し替え可能な設計（確定） / 外部API利用は設定画面で明示的に有効化（確定） / 外部APIがOFFの場合、AI編集メニューはグレーアウトし、ホバーで使えない旨を表示（確定） / 対象は本文のみ（確定） / 右クリックメニューは選択なしでも表示（確定） / 選択なしの場合はAI結果をカーソル位置へ挿入（確定） / 選択範囲がある場合はその範囲を置換（確定） / 全文対象のAI編集は「全文選択で右クリックした場合のみ」（確定） / カスタムプロンプト実行時は未選択でも全文対象として編集（確定） / AI編集ショートカットは選択あり/なしどちらでも実行可能（確定） / AI編集ショートカットはアプリ内のみ（確定） / ショートカット実行時も選択なしはカーソル位置へ挿入（確定） / AI編集ショートカットのデフォルト割り当ては無し（確定） / AI編集中は本文を編集不可（確定） / AI編集中はキャンセルで中断可能（確定） / AI編集中もEscでキャンセル可能（確定） / AI編集中のキャンセルは確認なしで即時終了（確定） / キャンセル時は本文は変更なし（確定） / AI編集中は「AIが編集中…」のテキストを表示し、「…」をアニメーション（確定） / AI編集プレビューは設定でON/OFF（確定） / プレビューON時のプレビューUIはモーダル（背景クリックでは閉じない、右上に×表示、×はキャンセル扱い）（確定） / プレビューON時は左右分割で差分表示（left: 元本文=グレー文字 / right: 修正後=緑の蛍光文字、デフォルト幅50/50、ドラッグで幅調整可、次回はデフォルト幅に戻す）（確定） / 修正後は変更箇所のみハイライト表示、全文対象時は全文表示（確定） / プレビューON時はプレビュー画面で再プロンプト送信して修正可能、Enterで確定反映（確定） / プレビューON時の再プロンプト送信中も「AIが編集中…」を表示（確定） / 再プロンプト送信中は「適用」ボタンを無効化（確定） / 再プロンプト送信中も「キャンセル」ボタンは押せる（確定） / 再プロンプト入力欄はプレビュー画面下部に固定（確定） / 再プロンプト送信キーは設定で切替（Enter送信 / Ctrl+Enter送信）、デフォルトはCtrl+Enter（確定） / Enter送信の場合はShift+Enterで改行、Ctrl+Enter送信の場合はEnterで改行（確定） / プレビューの確定はEnter、キャンセルはEscに加えてボタンでも操作可能（確定） / プレビューのボタン文言は「適用」「キャンセル」（確定） / プレビューON時に適用したら自動でプレビューを閉じて編集画面に戻る（確定） / プレビューON時にキャンセルしたら自動でプレビューを閉じて編集画面に戻る（確定） / プレビューOFF時は即時反映（Undoで戻せる）（確定） / プレビューOFF時のカスタムプロンプト実行はモーダルを出さず、該当プロンプト行に小さなローディング表示（中央×でキャンセル）を出す（確定） / 対象範囲は「全文/選択/カーソル」を表示し、トグルで切替可能（確定） / AI編集結果が空/同一の場合は何もしない＋通知（トースト右上・3秒、文言:「空もしくは変更がありませんでした。」）（確定） / AI編集が失敗した場合はトースト右上・3秒で通知（文言:「AI編集に失敗しました。」、リトライボタンなし）（確定） / カスタムプロンプトは最大6つで名前付き（確定） / 初期プリセットは2つ「翻訳」「構成」で登録し、ユーザーが自由に編集可能（確定） / 残り枠は空でユーザーが追加（確定） / 右クリックメニューにはカスタムプロンプトを直接表示（確定） / 上部は「カスタムプロンプト」アイコン1つにし、ホバーで一覧をアコーディオン表示（確定） / 一覧の項目押下で即実行（プレビュー設定に従う）（確定） / 上部ツールバーのアイコン順はCmd+ドラッグで並べ替え可能（端末内保存）（確定） / 非対応端末の扱い（TODO）
- 受け入れ条件（案）:
  - 右クリックでAI編集を呼び出せる（選択なしでも表示される）
  - AI編集の対象は本文のみ
  - 選択なしの場合、AI結果がカーソル位置へ挿入される
  - 全文対象のAI編集は、全文選択時のみ実行できる
  - AI編集ショートカットは選択あり/なしどちらでも実行できる
  - AI編集ショートカットはアプリ内のみで利用できる
  - AI編集中は本文を編集できない
  - AI編集中はキャンセルで中断できる
  - AI編集中もEscでキャンセルできる
  - AI編集中のキャンセルは確認なしで即時終了する
  - キャンセル時は本文は変更されない
  - 右クリックメニューにカスタムプロンプトが直接表示される
  - 上部に「カスタムプロンプト」アイコンが表示され、ホバーで一覧が出る
  - 上部アイコン押下で即実行される（プレビュー設定に従う）
  - AI編集結果が空/同一の場合は何もしない＋通知される（文言:「空もしくは変更がありませんでした。」）
  - AI編集が失敗した場合はトースト（右上・3秒、文言:「AI編集に失敗しました。」、リトライなし）で通知される
  - AI編集中は「AIが編集中…」のテキストを表示し、「…」がアニメーションする
  - ショートカット実行時も、選択なしはカーソル位置に挿入される
  - 設定でAI編集プレビューのON/OFFを切り替えられる
  - プレビューON時はモーダルで表示される
  - プレビューON時のモーダルは背景クリックでは閉じない
  - プレビューON時のモーダル右上に×が表示され、×はキャンセル扱い
  - 外部API利用は設定画面で明示的に有効化できる
  - 外部APIがOFFの場合、AI編集メニューはグレーアウトし、ホバーで使えない旨を表示する
  - 選択範囲がある場合はAI編集結果でその範囲が置換される
  - プレビューON時は左右分割で差分表示（左: 元本文=グレー文字 / 右: 修正後=緑の蛍光文字、デフォルト幅50/50、ドラッグで幅調整可、次回はデフォルト幅に戻る）
  - 修正後は変更箇所のみハイライト表示、全文対象時は全文表示される
  - プレビューON時はプレビュー画面が表示され、再プロンプト送信で修正できる
  - プレビューON時の再プロンプト送信中も「AIが編集中…」が表示される
  - 再プロンプト送信中は「適用」ボタンが無効化される
  - 再プロンプト送信中も「キャンセル」ボタンは押せる
  - 再プロンプト入力欄はプレビュー画面下部に固定されている
  - 再プロンプト送信キーは設定で切替できる（Enter送信 / Ctrl+Enter送信、デフォルトはCtrl+Enter）
  - Enter送信の場合はShift+Enterで改行できる
  - Ctrl+Enter送信の場合はEnterで改行できる
  - プレビュー画面のボタン文言は「適用」「キャンセル」
  - プレビューON時はEnterで確定し本文に反映される
  - プレビュー画面には確定/キャンセルのボタンがある（Enter/Escでも操作可能）
  - プレビューON時に適用したら自動でプレビューを閉じて編集画面に戻る
  - プレビューON時にキャンセルしたら自動でプレビューを閉じて編集画面に戻る
  - プレビューOFF時はプレビューなしで即時反映される
  - カスタムプロンプト（最大6）が実行できる（右クリック・ショートカット対応）
  - カスタムプロンプトは名前付きで、ユーザーが編集できる
  - 初期プリセットは「翻訳」「構成」で登録されている
  - 残り1枠は空で、ユーザーが追加できる
  - ローカル優先で動作し、外部API利用はユーザーが明示的にONできる

### 7) UI/UX調整

- 決定事項: TODO（改善対象の優先順位）
- 受け入れ条件（案）:
  - 主要導線（メモ作成/編集/検索/クイック起動）が迷わず使える

### 8) タブ機能（複数メモを同時に開く）

- 決定事項:
  - 複数のメモをタブで開き、切り替え可能（確定）
  - タブ機能はiOSも対応（確定）
- 受け入れ条件（案）:
  - 複数メモを開くとタブが増え、タブで切り替えできる

### 9) ドラッグ&ドロップ（タブ → フォルダ）

- 決定事項:
  - サイドバー表示中に、任意のメモのタブをドラッグ&ドロップでフォルダへ移動/保存できる（確定）
  - 保存済みメモをドロップした場合は「フォルダに移動」or「フォルダに複製」を選べる（確定）
  - クイックメモをドロップした場合は、そのフォルダに保存する（確定）
  - タイトル未決定の場合は、ドロップ時にタイトル入力ポップアップを出して保存（確定）
  - 「複製」を選んだ場合、タイトル入力ポップアップを出し、初期値は「元タイトル + コピー」で入力済み（確定）
- 受け入れ条件（案）:
  - D&Dでフォルダへ移動/保存できる
  - 保存済みメモは「移動/複製」を選択できる
  - クイックメモはドロップで保存される
  - タイトル未決定の場合、タイトル入力なしでは保存できない
  - 複製の場合、タイトル入力ポップアップが出て、初期値のままでも保存できる（編集も可能）
- メモ:
  - 「複製」は新規メモとして作成（新しいID）する想定
- 対象プラットフォーム（iOSでD&Dをやるか）はmacOS実装後に決定
  - タイトル重複時はエラーで保存不可（確定）

---

## 保留事項（macOS実装後に決定）

1) iOSでもドラッグ&ドロップでフォルダ移動をやるか
2) iOSのタブは最大いくつまで開けるか

---

## リスクと対応策（ドラフト）

- フォルダ削除の誤操作: 影響が大きい → 警告で件数を出し、明示同意がない限り削除しない
- タブ+D&D: 実装とUIの複雑性が高い → 対象OSと最小要件（最初はmacOSのみ等）を先に確定する
- Apple Intelligence（オフライン）: 端末/OS要件がある → 対応範囲を明記し、非対応時のフォールバックを用意する
- iOS詳細の後回し: macOSとの差分が出やすい → macOS完了後にiOSの差分だけ最小で決める

---

## 完了条件（このPLANの完了）

- macOSに関する「決定事項」と「受け入れ条件」が埋まっている
- iOSの保留事項が明記され、後続で決定することが合意されている
- 各機能の「決定事項」と「受け入れ条件」が埋まっている
- スコープ外（Obsidian/Apple Watch/色ラベル）が明記され、関係者が合意している
