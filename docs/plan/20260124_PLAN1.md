# プランニングドキュメント：Apple Intelligence API 呼び出し実装（macOS先行）

## 共通ルール（必読）

- 共通ルールはリポジトリルートの `AGENTS.md` を参照: `../../AGENTS.md`

## 今回の追加ルール / 例外（差分だけを書く）

- 対象プラットフォーム: macOSのみ（iOSは後続で別途検討）
- 実装と合わせて「実機での動作テスト」まで実施する
- Apple Intelligence 非対応時のフォールバックは設定画面でON/OFF可能にする

## 実装モード（単独 / 並列）

- 実装モード: 単独
- 判定理由（要点だけ）:
  - 分割の独立性（契約で切れる）: NG（Flutter/設定/ネイティブ連携が密結合）
  - 合流コスト（共有境界の多さ）: 高
  - 編集衝突（同じファイル/意思決定の奪い合い）: 高
- 境界ケース（迷う/判断が曖昧）: **単独を選ぶ（既定動作）**

**作成日**: 2026-01-24
**バージョン**: 1.0
**ステータス**: 実装中
**前バージョンからの変更**: 新規作成（承認済み）

---
# 進捗状況

## ⚡ クイックリファレンス

**現在地**: Phase 2 - 実装（Flutter/ネイティブ/設定）
**次のアクション**: タグ辞書同期の動作確認（端末間で反映を確認）
**ブロッカー**: なし
**重要な決定事項**: macOS先行・実機テストまで行う / Apple Intelligence優先・外部APIはフォールバック / 設定でApple Intelligenceと外部APIを個別にON/OFF / タグ辞書を参照して既存タグ優先 / タグ提案は最大5件 / ホームにタグ管理導線を追加 / AI処理中でも本文入力はロックしない / タグ辞書はSupabaseのtag_dictionaryテーブルで同期（適用済み・onConflict整合のためuser_id+canonical_tagを全体一意）

### フェーズ進捗
- [x] Phase 0: 仕様整理
- [x] Phase 1: 設計（I/F・設定・フォールバック）
- [🔄] Phase 2: 実装（Flutter/ネイティブ/設定）
- [ ] Phase 3: 動作テスト（実機）

---
# 実装内容

## 要件概要

### 背景・目的
- 既存のAI呼び出しをApple Intelligence（ローカル）優先で行えるようにする

### 実装する機能
- **Apple IntelligenceのAPI呼び出し**（macOS）
- **設定画面でのフォールバックON/OFF**
- **実機での動作テスト**

### スコープ外
- iOS実装（後続）
- 仕様外のUI追加

---
## アーキテクチャ設計（最小）

### 影響範囲（想定）
- `lib/services/ai_service.dart`（呼び出し口の差し替え/分岐）
- `lib/presentation/screens/settings_screen.dart`（設定UI追加）
- `lib/domain/app_settings.dart` / `lib/presentation/providers/app_settings_controller.dart`（設定保持）
- `macos/` 配下（Apple Intelligence呼び出しのネイティブ実装）

### 方式（TODO）
- Flutter ⇔ macOS は MethodChannel で連携
- Apple Intelligence API は Foundation Models framework を利用（文章編集 / タグ提案）
- 非対応端末の検知は availability を利用

---
## 実装フェーズ

### Phase 0: 仕様整理
- Apple Intelligence API のI/O/制約/対応OSを確定（一次情報）
- 対象機能は AI編集 / タグ提案 を含める

### Phase 1: 設計
- 既存 `AiService` の切替ポイント定義（Apple Intelligence優先 + フォールバック）
- 設定項目の定義（Apple Intelligence / 外部API を個別にON/OFF）
- 失敗時のエラーハンドリング方針整理

### Phase 2: 実装
- macOS ネイティブ側の呼び出し実装
- Flutter 側の呼び出し統合（AI編集/必要ならタグ提案）
- 設定画面へのトグル追加

### Phase 3: 動作テスト
- macOS実機での呼び出し確認
- フォールバックON/OFFの挙動確認

---
## リスクと対応策

- **API仕様の不確実性**: 仕様確定前に実装しない（Phase 0で確定）
- **非対応端末**: 設定のフォールバックON/OFFで挙動を明示

---
## 完了条件

- Apple Intelligence API がmacOSで呼び出せる
- 設定画面でフォールバックのON/OFFが切り替えられる
- 実機での動作テスト結果が記録できる

---
## 未確定事項（Open）

- なし
